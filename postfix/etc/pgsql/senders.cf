hosts = postgresql://mail:${MAIL_DB_PASSWORD}@127.0.0.1/mail?sslmode=disable

query = SELECT username || '@' || domain AS login FROM accounts WHERE username = '%u' AND domain = '%d' AND active
        UNION
        SELECT destination AS login FROM aliases WHERE source = '%s' AND active
